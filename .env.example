# ThermalFaultAnalyzer Environment Variables
# Copy this file to .env and fill in your values

# Google Gemini API key (optional — falls back to template summaries if not set)
GEMINI_API_KEY=your_gemini_api_key_here

# Flask settings (optional)
FLASK_ENV=development
FLASK_DEBUG=true
